# Example MCP Tool Call: `list_comfyui_workflows`

```json
{}
```

This returns:
- configured workflows from `config.json`
- discovered `.json` files inside `workflows/`
- warnings for missing workflow files or unconfigured files
